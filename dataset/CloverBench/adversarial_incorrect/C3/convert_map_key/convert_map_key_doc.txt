Converts the keys of the inputs map using the function f. Require that f is an injective function from domain natural numbers to range natural numbers. Ensure that returned new map r has f(k) as the keys for every key k in f.
