// specification: Rotate the non-empty array by the offset to the right. The offset is upper bounded by the length of the input array.
method rotate(a: array<int>, offset:int) returns (b: array<int> )
//TOFILL
