// specification: Copies `len` elements from `src` array starting at `sStart` to `dest` array starting at `dStart` and returns the modified `dest` array; maintains original `dest` elements outside copied range.
method copy( src: array<int>, sStart: nat, dest: array<int>, dStart: nat, len: nat) returns (r: array<int>)
//TOFILL
