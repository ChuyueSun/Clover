// specification: Converts the keys of the inputs map using the function f, ensuring unique mapping, and returns the new map r.
method convert_map_key(inputs: map<nat, bool>, f: nat->nat) returns(r:map<nat, bool>)
//TOFILL
