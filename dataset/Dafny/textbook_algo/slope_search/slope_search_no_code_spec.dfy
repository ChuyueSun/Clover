// specification: Search for the index of an existing key in a slope represented by an 2-D array. Require the slope to be a plane whose height is non-decreasing on the same x-axis (horizontal line) or same y-axis (vertical line). Require that such an element exists.
method SlopeSearch(a: array2<int>, key: int) returns (m:int, n:int)
//TOFILL
